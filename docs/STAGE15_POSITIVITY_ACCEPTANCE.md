# Acceptance criteria

The positivity implementation is acceptable only when every synthetic check passes，the public PC and mobile browser regression tests remain green，and no public wrapper imports the isolated Stage 15 module．A failing check must be diagnosed before changing any approved geometry or physical input．
